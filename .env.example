# OilGas Nanobot Swarm Configuration
# Copy to .env and fill in values
# Powered by VibeCaaS.com — a division of NeuralQuantum.ai LLC

# ── AI Backend ─────────────────────────────────────────────────────────────────
# Anthropic Claude (recommended — cloud, no GPU required)
ANTHROPIC_API_KEY=sk-ant-your-key-here
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# vLLM backend (optional — local GPU inference)
VLLM_URL=http://localhost:8000/v1
VLLM_API_KEY=your-vllm-api-key
SWARM_MODEL=nanobot-reasoner

# ── Gateway ────────────────────────────────────────────────────────────────────
GATEWAY_API_KEY=your-gateway-api-key

# ── Redis State Store ──────────────────────────────────────────────────────────
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password
REDIS_DB=0

# ── Oil & Gas Features ─────────────────────────────────────────────────────────
# Enable oil & gas agent teams (well-engineering-review, reservoir-analysis, etc.)
ENABLE_OILGAS_TEAMS=true

# ── Knowledge Vault (optional) ────────────────────────────────────────────────
# Path to Obsidian-compatible markdown vault for persistent knowledge
VAULT_PATH=~/.nellie/vault

# ── Optional Integrations ─────────────────────────────────────────────────────
# Microsoft 365 sync (contacts, email, calendar, tasks)
# MS_GRAPH_CLIENT_ID=your-azure-app-id
# MS_GRAPH_CLIENT_SECRET=your-client-secret
# MS_GRAPH_TENANT_ID=your-tenant-id

# OpenAI embeddings (upgrade from hash-based to semantic vector search)
# OPENAI_API_KEY=sk-your-openai-key
